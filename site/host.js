const toast=document.querySelector('#toast');function showToast(t){toast.textContent=t;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)}document.querySelectorAll('[data-action]').forEach(b=>b.onclick=async()=>{const r=await fetch('/api/host/request',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:b.dataset.id,action:b.dataset.action})});if(r.ok){b.closest('.host-song').remove();showToast(b.dataset.action==='approve'?'Song approved':'Request declined')}});document.querySelectorAll('[data-moment]').forEach(b=>b.onclick=()=>showToast(`${b.dataset.moment} is ready ✦`));
