{
  "name": "qara-wedding-music-queue",
  "version": "1.0.0",
  "main": "server.mjs",
  "type": "module",
  "description": "Private wedding music request queue with guest and host views.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs && node --check app.js && node --check host.js"
  },
  "keywords": [
    "wedding",
    "music",
    "queue",
    "song-requests"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
